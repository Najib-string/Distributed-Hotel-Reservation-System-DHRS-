services:
  nginx:
    image: nginx:1.25
    container_name: nginx
    ports:
      - "15432:15432"
      - "17017:17017"
      - "16379:16379"
    volumes:
      - ./config/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - distrib-net
    depends_on:
      - pg-jakarta
      - pg-surabaya
      - pg-makassar
      - mongo-primary
      - mongo-secondary
      - redis-master

  pg-jakarta:
    image: postgres:16
    container_name: pg-jakarta
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: db_jakarta
    ports:
      - "5432:5432"
    volumes:
      - pg_jakarta_data:/var/lib/postgresql/data
    networks:
      - distrib-net

  pg-surabaya:
    image: postgres:16
    container_name: pg-surabaya
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: db_surabaya
    ports:
      - "5433:5432"
    volumes:
      - pg_surabaya_data:/var/lib/postgresql/data
    networks:
      - distrib-net

  pg-makassar:
    image: postgres:16
    container_name: pg-makassar
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: db_makassar
    ports:
      - "5434:5432"
    volumes:
      - pg_makassar_data:/var/lib/postgresql/data
    networks:
      - distrib-net

  mongo-primary:
    image: mongo:7
    container_name: mongo-primary
    command: ["--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongo_primary_data:/data/db
    networks:
      - distrib-net

  mongo-secondary:
    image: mongo:7
    container_name: mongo-secondary
    command: ["--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27018:27017"
    volumes:
      - mongo_secondary_data:/data/db
    networks:
      - distrib-net

  redis-master:
    image: redis:7
    container_name: redis-master
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    ports:
      - "6379:6379"
    volumes:
      - ./config/redis/master.conf:/usr/local/etc/redis/redis.conf:ro
      - redis_master_data:/data
    networks:
      - distrib-net

  redis-replica:
    image: redis:7
    container_name: redis-replica
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    ports:
      - "6380:6379"
    volumes:
      - ./config/redis/replica.conf:/usr/local/etc/redis/redis.conf:ro
      - redis_replica_data:/data
    networks:
      - distrib-net
    depends_on:
      - redis-master

networks:
  distrib-net:
    driver: bridge

volumes:
  pg_jakarta_data:
  pg_surabaya_data:
  pg_makassar_data:
  mongo_primary_data:
  mongo_secondary_data:
  redis_master_data:
  redis_replica_data: